<div class="nested-fields">
    <%= f.label :time %><br>
    <%= f.time_field :time %>

    <%= f.label :boarding_point %><br>
    <%= f.collection_select :boarding_point_id, BoardingPoint.all, :id, :name, prompt: true %>

    <%= f.hidden_field :_destroy %>
    <%= link_to_remove_association 'Remove Boarding', f %>
</div>
